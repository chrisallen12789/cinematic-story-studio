# This file is documentation only; root scripts do not automatically load .env.
# Set these optional, non-secret overrides in the invoking process when needed.
CSS_BOOTSTRAP_PYTHON=
CSS_PYINSTALLER_MODE=
